pipeline {
          agent {
		  label {
		  label "built-in"
          customWorkspace "/mnt/git"		  
		  }
		  }

stages {

        stage ("install httpd") {
		steps {
		sh "cp index.html /var/www/html"
		
		
		}
		
		}


}



}
